definition {

	@summary = "Default summary"
	macro _gotoPage(pageName = null) {
		var key_pageName = ${pageName};
		var count = 0;

		while ((${count} != 3) && (IsElementNotPresent(locator1 = "Home#PAGE_ACTIVE"))) {
			AssertClick.assertPartialTextClickAt(
				locator1 = "Home#PAGE",
				value1 = ${pageName});

			var count = ${count} + 1;
		}

		AssertVisible(locator1 = "Home#PAGE_ACTIVE");
	}

	@summary = "Default summary"
	macro _isLocalizedURL(locale = null) {
		var currentURL = Navigator.getCurrentURL();
		var portalURL = JSONCompany.getPortalURL();
		var isLocalizedURL = "false";

		if (contains(${currentURL}, "${portalURL}/${locale}")) {
			var isLocalizedURL = "true";
		}

		return ${isLocalizedURL};
	}

	@summary = "Default summary"
	macro closeSpecificWindow(closeWindowTitle = null, selectWindowTitle = null) {
		Navigator.selectSpecificWindow(windowTitle = ${closeWindowTitle});

		Close.closeAndSelectWindow(value1 = "title=${selectWindowTitle}");
	}

	@summary = "Default summary"
	macro closeWindow() {
		Close.closeWindow();
	}

	@summary = "Default summary"
	macro ensureLocaleURL(locale = null) {
		var isLocalizedURL = Navigator._isLocalizedURL(locale = ${locale});

		if (${isLocalizedURL} == "false") {
			var currentURL = Navigator.getCurrentURL();
			var portalURL = JSONCompany.getPortalURL();

			Navigator.openSpecificURL(url = StringUtil.replace(${currentURL}, ${portalURL}, "${portalURL}/${locale}"));
		}
	}

	@summary = "Default summary"
	macro getCurrentURL() {
		var currentURL = selenium.getLocation();

		return ${currentURL};
	}

	@summary = "Default summary"
	macro gotoAccountSettings() {
		UserBar.gotoDropdownItem(dropdownItem = "Account Settings");
	}

	@summary = "Default summary"
	macro gotoBack() {
		Click(locator1 = "Icon#BACK");
	}

	@summary = "Default summary"
	macro gotoEditPageViaControlMenu(pageName = null) {
		Click(locator1 = "ControlMenu#EDIT_COG");

		AssertTextEquals(
			locator1 = "Portlet#HEADER",
			value1 = ${pageName});
	}

	@summary = "Default summary"
	macro gotoLiveView() {
		if (isSet(remoteStaging)) {
			Click(
				key_navItem = "Go to Remote Live",
				locator1 = "NavBar#NAV_ITEM_REDIRECTION");
		}
		else {
			Click(
				key_navItem = "Live",
				locator1 = "NavBar#NAV_ITEM_REDIRECTION");
		}
	}

	@summary = "Default summary"
	macro gotoLoginPage() {
		AssertClick(
			locator1 = "UserBar#USER_SIGN_IN",
			value1 = "Sign In");
	}

	@summary = "Default summary"
	macro gotoNavItem(navItem = null) {
		var key_navItem = ${navItem};

		AssertClick(
			locator1 = "NavBar#NAV_ITEM_LINK",
			value1 = ${navItem});

		AssertElementPresent(locator1 = "NavBar#ACTIVE_NAV_ITEM_LINK");
	}

	@summary = "Default summary"
	macro gotoNavNested(navNested = null) {
		var key_navNested = ${navNested};

		AssertClick.assertPartialTextClickAt(
			locator1 = "NavNested#NAV_NESTED_LINK",
			value1 = ${navNested});

		AssertElementPresent(locator1 = "NavNested#ACTIVE_NAV_NESTED_LINK");
	}

	@summary = "Default summary"
	macro gotoNavTab(navTab = null) {
		var key_tab = ${navTab};

		if (IsElementNotPresent(locator1 = "NavTab#ACTIVE_TAB_LINK")) {
			AssertClick.assertPartialTextClickAt(
				locator1 = "NavTab#TAB_LINK",
				value1 = ${navTab});
		}

		AssertElementPresent(locator1 = "NavTab#ACTIVE_TAB_LINK");

		WaitForLiferayEvent.initializeLiferayEventLog();
	}

	@summary = "Default summary"
	macro gotoNavUnderline(navUnderline = null) {
		var key_navUnderline = ${navUnderline};

		if (IsElementNotPresent(locator1 = "NavUnderline#ACTIVE_NAV_UNDERLINE_LINK")) {
			AssertClick(
				locator1 = "NavUnderline#NAV_UNDERLINE_LINK",
				value1 = ${navUnderline});
		}

		AssertElementPresent(locator1 = "NavUnderline#ACTIVE_NAV_UNDERLINE_LINK");
	}

	@summary = "Default summary"
	macro gotoNotifications() {
		UserBar.gotoDropdownItem(dropdownItem = "Notifications");
	}

	@summary = "Default summary"
	macro gotoPage(baseURL = null, pageName = null) {
		Navigator.openURL(baseURL = ${baseURL});

		Pause(value1 = 3000);

		Navigator._gotoPage(pageName = ${pageName});
	}

	@summary = "Default summary"
	macro gotoPersonalSite() {
		UserBar.gotoDropdownItem(dropdownItem = "My Profile");
	}

	@summary = "Default summary"
	macro gotoPrivateSitePage(baseURL = null, siteName = null, pageName = null) {
		Navigator.openPrivateSiteURL(
			baseURL = ${baseURL},
			siteName = ${siteName});

		Navigator._gotoPage(pageName = ${pageName});
	}

	@summary = "Default summary"
	macro gotoPrivateStagedSitePage(baseURL = null, siteName = null, pageName = null) {
		Navigator.openPrivateStagedSiteURL(
			baseURL = ${baseURL},
			siteNameURL = ${siteName});

		Navigator._gotoPage(pageName = ${pageName});
	}

	@summary = "Default summary"
	macro gotoSidebarTab(tabName = null) {
		var key_tabName = ${tabName};

		AssertElementPresent(locator1 = "Sidebar#TAB");

		if (IsElementNotPresent(locator1 = "Sidebar#ACTIVE_TAB")) {
			Click(locator1 = "Sidebar#TAB");
		}

		AssertElementPresent(locator1 = "Sidebar#ACTIVE_TAB");

		WaitForLiferayEvent.initializeLiferayEventLog();
	}

	@summary = "Default summary"
	macro gotoSitePage(baseURL = null, siteName = null, pageName = null) {
		Navigator.openSiteURL(
			baseURL = ${baseURL},
			siteName = ${siteName});

		Navigator._gotoPage(pageName = ${pageName});
	}

	@summary = "Default summary"
	macro gotoSpecificURLPage(pageName = null, url = null) {
		Navigator.openSpecificURL(url = ${url});

		Navigator._gotoPage(pageName = ${pageName});
	}

	@summary = "Default summary"
	macro gotoStagedSitePage(baseURL = null, siteName = null, defaultSite = null, pageName = null) {
		Navigator.openStagingSiteURL(
			baseURL = ${baseURL},
			defaultSite = ${defaultSite},
			siteName = ${siteName});

		Navigator._gotoPage(pageName = ${pageName});
	}

	@summary = "Default summary"
	macro gotoStagedView() {
		ProductMenuHelper.openProductMenu();

		Click(locator1 = "Link#STAGING");

		WaitForLiferayEvent.initializeLiferayEventLog();
	}

	@summary = "Default summary"
	macro openNodePort(nodePort = null) {
		Open(value1 = "http://localhost:${nodePort}");
	}

	@summary = "Default summary"
	macro openPrivateSitePageURL(siteName = null, pageName = null) {
		var siteNameURL = StringUtil.replace(${siteName}, " ", "-");

		var siteNameURL = StringUtil.lowerCase(${siteNameURL});
		var pageNameURL = StringUtil.replace(${pageName}, " ", "-");

		var pageNameURL = StringUtil.lowerCase(${pageNameURL});
		var baseURL = ${baseURL};

		if (!(isSet(baseURL))) {
			var baseURL = JSONCompany.getPortalURL();
		}

		Open(value1 = "${baseURL}/group/${siteNameURL}/${pageNameURL}");
	}

	@summary = "Default summary"
	macro openPrivateSiteURL(siteName = null) {
		var siteNameURL = StringUtil.replace(${siteName}, " ", "-");

		var siteNameURL = StringUtil.lowerCase(${siteNameURL});
		var baseURL = ${baseURL};

		if (!(isSet(baseURL))) {
			var baseURL = JSONCompany.getPortalURL();
		}

		Open(value1 = "${baseURL}/group/${siteNameURL}");
	}

	@summary = "Default summary"
	macro openPrivateStagedSiteURL() {
		var siteNameURL = StringUtil.replace(${siteNameURL}, " ", "-");

		var siteNameURL = StringUtil.lowerCase(${siteNameURL});
		var baseURL = ${baseURL};

		if (!(isSet(baseURL))) {
			var baseURL = JSONCompany.getPortalURL();
		}

		Open(value1 = "${baseURL}/group/${siteNameURL}-staging");
	}

	@summary = "Default summary"
	macro openPrivateURL() {
		var baseURL = ${baseURL};

		if (!(isSet(baseURL))) {
			var baseURL = JSONCompany.getPortalURL();
		}

		Open(value1 = "${baseURL}/group/guest");
	}

	@summary = "Default summary"
	macro openSitePage(siteName = null, pageName = null) {
		var siteNameURL = StringUtil.replace(${siteName}, " ", "-");

		var siteNameURL = StringUtil.lowerCase(${siteNameURL});
		var baseURL = ${baseURL};

		if (!(isSet(baseURL))) {
			var baseURL = JSONCompany.getPortalURL();
		}

		var pageNameURL = StringUtil.replace(${pageName}, " ", "-");

		var pageNameURL = StringUtil.lowerCase(${pageNameURL});

		Open(value1 = "${baseURL}/web/${siteNameURL}/${pageNameURL}");
	}

	@summary = "Default summary"
	macro openSiteStagingConfiguration(siteName = null) {
		var siteNameURL = StringUtil.replace(${siteName}, " ", "-");

		var siteNameURL = StringUtil.lowerCase(${siteNameURL});
		var baseURL = ${baseURL};

		if (!(isSet(baseURL))) {
			var baseURL = JSONCompany.getPortalURL();
		}

		Navigator.openWithAppendToBaseURL(urlAppend = "group/${siteNameURL}-staging/~/control_panel/manage?p_p_id=com_liferay_staging_processes_web_portlet_StagingProcessesPortlet");
	}

	@summary = "Default summary"
	macro openSiteURL(siteName = null) {
		var siteNameURL = StringUtil.replace(${siteName}, " ", "-");

		var siteNameURL = StringUtil.lowerCase(${siteNameURL});
		var baseURL = ${baseURL};

		if (!(isSet(baseURL))) {
			var baseURL = JSONCompany.getPortalURL();
		}

		Open(value1 = "${baseURL}/web/${siteNameURL}");
	}

	@summary = "Default summary"
	macro openSpecificURL(domain = null, url = null) {
		Open(value1 = ${url});

		Alert.confirmSecurity(domain = ${domain});
	}

	@summary = "Default summary"
	macro openStagingSiteURL(siteName = null) {
		var siteNameURL = StringUtil.replace(${siteName}, " ", "-");

		var siteNameURL = StringUtil.lowerCase(${siteNameURL});
		var baseURL = ${baseURL};

		if (!(isSet(baseURL))) {
			var baseURL = JSONCompany.getPortalURL();
		}

		if (isSet(defaultSite)) {
			Open(value1 = "${baseURL}/web/guest-staging");
		}
		else {
			Open(value1 = "${baseURL}/web/${siteNameURL}-staging");
		}
	}

	@summary = "Default summary"
	macro openStoreURL() {
		Navigator.openWithAppendToBaseURL(urlAppend = "group/control_panel/manage?p_p_id=com_liferay_marketplace_store_web_portlet_MarketplaceStorePortlet");
	}

	@summary = "Default summary"
	macro openToGroupPagesPortlet(groupName = null, site = null, portalURL = null, siteURLKey = null, portlet = null, layoutName = null) {
		var plid = JSONLayout.getPlid(
			groupName = ${groupName},
			layoutName = ${layoutName},
			portalURL = ${portalURL},
			site = ${site});

		if (${portlet} == "General") {
			var screenNavigationEntryKey = "general";
		}

		if (${portlet} == "Open Graph") {
			var screenNavigationEntryKey = "open-graph";
		}

		if (${portlet} == "SEO") {
			var screenNavigationEntryKey = "seo";
		}

		var baseURL = ${baseURL};

		if (!(isSet(baseURL))) {
			var baseURL = JSONCompany.getPortalURL();
		}

		Open(value1 = "${baseURL}/group/${siteURLKey}/~/control_panel/manage?p_p_id=com_liferay_layout_admin_web_portlet_GroupPagesPortlet&_com_liferay_layout_admin_web_portlet_GroupPagesPortlet_tabs1=pages&_com_liferay_layout_admin_web_portlet_GroupPagesPortlet_privateLayout=false&_com_liferay_layout_admin_web_portlet_GroupPagesPortlet_displayStyle=miller-columns&_com_liferay_layout_admin_web_portlet_GroupPagesPortlet_mvcRenderCommandName=%2Flayout_admin%2Fedit_layout&_com_liferay_layout_admin_web_portlet_GroupPagesPortlet_portletResource=com_liferay_layout_admin_web_portlet_GroupPagesPortlet&p_r_p_selPlid=${plid}&_com_liferay_layout_admin_web_portlet_GroupPagesPortlet_screenNavigationEntryKey=${screenNavigationEntryKey}");
	}

	@summary = "Default summary"
	macro openURL() {
		WaitForSPARefresh();

		var baseURL = ${baseURL};

		if (!(isSet(baseURL))) {
			var baseURL = JSONCompany.getPortalURL();
		}

		Open(value1 = "${baseURL}/web/guest/home");
	}

	@summary = "Default summary"
	macro openWithAppendToBaseURL(urlAppend = null) {
		if (!(isSet(baseURL))) {
			var baseURL = JSONCompany.getPortalURL();
		}

		Open(value1 = "${baseURL}/${urlAppend}");
	}

	@summary = "Default summary"
	macro selectSpecificWindow(windowTitle = null) {
		SelectWindow(value1 = "title=${windowTitle}");
	}

	@summary = "Default summary"
	macro viewActivatedNavTab(navTab = null) {
		AssertElementPresent(
			key_tab = ${navTab},
			locator1 = "NavTab#ACTIVE_TAB_LINK");
	}

	@summary = "Default summary"
	macro viewNavTab(navTab = null) {
		AssertVisible(
			key_tab = ${navTab},
			locator1 = "NavTab#TAB_LINK");
	}

	@summary = "Default summary"
	macro viewNoNavTab(navTab = null) {
		AssertElementNotPresent(
			key_tab = ${navTab},
			locator1 = "NavTab#TAB_LINK");
	}

}