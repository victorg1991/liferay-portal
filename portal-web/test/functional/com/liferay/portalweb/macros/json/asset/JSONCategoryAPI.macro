definition {

	@summary = "Default summary"
	macro _addCategory(vocabularyId = null, groupId = null, parentCategoryId = null, descriptionMap = null, serviceContext = null, titleMap = null) {
		Variables.assertDefined(parameterList = "${groupId},${parentCategoryId},${titleMap},${descriptionMap},${vocabularyId},${serviceContext}");

		var portalURL = JSONCompany.getPortalURL();
		var userLoginInfo = JSONUtil2.formatJSONUser();

		var curl = '''
			${portalURL}/api/jsonws/assetcategory/add-category \
				-u ${userLoginInfo} \
				-d groupId=${groupId} \
				-d parentCategoryId=${parentCategoryId} \
				-d titleMap=${titleMap} \
				-d descriptionMap=${descriptionMap} \
				-d vocabularyId=${vocabularyId} \
				-d categoryProperties= \
				-d serviceContext=${serviceContext}
		''';

		var categoryId = JSONCurlUtil.post(${curl}, "$.categoryId");

		return ${categoryId};
	}

	@summary = "Default summary"
	macro _addVocabulary(groupId = null, descriptionMap = null, serviceContext = null, titleMap = null, visibilityType = null) {
		Variables.assertDefined(parameterList = "${groupId},${titleMap},${descriptionMap},${serviceContext},${visibilityType}");

		var portalURL = JSONCompany.getPortalURL();
		var userLoginInfo = JSONUtil2.formatJSONUser();

		var curl = '''
			${portalURL}/api/jsonws/assetvocabulary/add-vocabulary \
				-u ${userLoginInfo} \
				-d externalReferenceCode = \
				-d groupId=${groupId} \
				-d title= \
				-d titleMap=${titleMap} \
				-d descriptionMap=${descriptionMap} \
				-d Settings= \
				-d visibilityType=${visibilityType} \
				-d serviceContext=${serviceContext}
		''';

		com.liferay.poshi.runner.util.JSONCurlUtil.post(${curl});
	}

	@summary = "Default summary"
	macro _deleteCategory(categoryId = null) {
		Variables.assertDefined(parameterList = ${categoryId});

		var portalURL = JSONCompany.getPortalURL();
		var userLoginInfo = JSONUtil2.formatJSONUser();

		var curl = '''
			${portalURL}/api/jsonws/assetcategory/delete-category \
				-u ${userLoginInfo} \
				-d categoryId=${categoryId}
		''';

		com.liferay.poshi.runner.util.JSONCurlUtil.post(${curl});
	}

	@summary = "Default summary"
	macro _deleteVocabulary(vocabularyId = null) {
		Variables.assertDefined(parameterList = ${vocabularyId});

		var portalURL = JSONCompany.getPortalURL();
		var userLoginInfo = JSONUtil2.formatJSONUser();

		var curl = '''
			${portalURL}/api/jsonws/assetvocabulary/delete-vocabulary \
				-u ${userLoginInfo} \
				-d vocabularyId=${vocabularyId}
		''';

		com.liferay.poshi.runner.util.JSONCurlUtil.post(${curl});
	}

	@summary = "Default summary"
	macro _getCategoryExternalReferenceCodeByName(vocabularyId = null, groupId = null, categoryName = null) {
		Variables.assertDefined(parameterList = "${groupId},${categoryName},${vocabularyId}");

		var portalURL = JSONCompany.getPortalURL();
		var userLoginInfo = JSONUtil2.formatJSONUser();

		var curl = '''
			${portalURL}/api/jsonws/assetcategory/get-vocabulary-categories \
				-u ${userLoginInfo} \
				-d groupId=${groupId} \
				-d <CURL_DATA[name=${categoryName}]CURL_DATA> \
				-d vocabularyId=${vocabularyId} \
				-d <CURL_DATA[start=-1]CURL_DATA> \
				-d <CURL_DATA[end=-1]CURL_DATA> \
				-d -orderByComparator=
		''';

		var externalReferenceCode = JSONCurlUtil.post(${curl}, "$.[?(@['titleCurrentValue'] == '${categoryName}')]['externalReferenceCode']");

		if (${externalReferenceCode} == "") {
			fail("FAIL. Cannot find category.");
		}

		return ${externalReferenceCode};
	}

	@summary = "Default summary"
	macro _getCategoryFieldValueById(categoryId = null, fieldName = null) {
		Variables.assertDefined(parameterList = "${categoryId},${fieldName}");

		var portalURL = JSONCompany.getPortalURL();
		var userLoginInfo = JSONUtil2.formatJSONUser();

		var curl = '''
			${portalURL}/api/jsonws/assetcategory/get-category \
				-u ${userLoginInfo} \
				-d categoryId=${categoryId}
		''';

		var fieldValue = JSONCurlUtil.post(${curl}, "$.['${fieldName}']");

		if (${fieldValue} == "") {
			fail("FAIL. Cannot find category field value.");
		}

		return ${fieldValue};
	}

	@summary = "Default summary"
	macro _getCategoryIdByName(vocabularyId = null, groupId = null, categoryName = null) {
		Variables.assertDefined(parameterList = "${groupId},${categoryName},${vocabularyId}");

		var portalURL = JSONCompany.getPortalURL();
		var userLoginInfo = JSONUtil2.formatJSONUser();

		var curl = '''
			${portalURL}/api/jsonws/assetcategory/get-vocabulary-categories \
				-u ${userLoginInfo} \
				-d groupId=${groupId} \
				-d <CURL_DATA[name=${categoryName}]CURL_DATA> \
				-d vocabularyId=${vocabularyId} \
				-d <CURL_DATA[start=-1]CURL_DATA> \
				-d <CURL_DATA[end=-1]CURL_DATA> \
				-d -orderByComparator=
		''';

		var categoryId = JSONCurlUtil.post(${curl}, "$.[?(@['titleCurrentValue'] == '${categoryName}')]['categoryId']");

		if (${categoryId} == "") {
			fail("FAIL. Cannot find category.");
		}

		return ${categoryId};
	}

	@summary = "Default summary"
	macro _getVocabularyCategoriesIds(vocabularyId = null) {
		Variables.assertDefined(parameterList = ${vocabularyId});

		var portalURL = JSONCompany.getPortalURL();
		var userLoginInfo = JSONUtil2.formatJSONUser();

		var curl = '''
			${portalURL}/api/jsonws/assetcategory/get-vocabulary-categories \
				-u ${userLoginInfo} \
				-d vocabularyId=${vocabularyId} \
				-d "start=-1" \
				-d "end=-1" \
				-d -orderByComparator=
		''';

		echo("# Get Categories IDs");

		echo("curl: ${curl}");

		var categoriesId = JSONCurlUtil.post(${curl}, "$..['categoryId']");

		echo("categoriesId: ${categoriesId}");

		return ${categoriesId};
	}

	@summary = "Default summary"
	macro _getVocabularyIdByName(groupId = null, vocabularyName = null) {
		Variables.assertDefined(parameterList = "${groupId},${vocabularyName}");

		var portalURL = JSONCompany.getPortalURL();
		var userLoginInfo = JSONUtil2.formatJSONUser();

		var curl = '''
			${portalURL}/api/jsonws/assetvocabulary/get-group-vocabularies \
				-u ${userLoginInfo} \
				-d groupId=${groupId}
		''';

		var vocabularyId = JSONCurlUtil.post(${curl}, "$.[?(@['titleCurrentValue'] == '${vocabularyName}')]['vocabularyId']");

		if (${vocabularyId} == "") {
			fail("FAIL. Cannot find vocabulary.");
		}

		return ${vocabularyId};
	}

	@summary = "Default summary"
	macro _updateCategory(vocabularyId = null, parentCategoryId = null, descriptionMap = null, titleMap = null, categoryId = null, externalReferenceCode = null) {
		Variables.assertDefined(parameterList = "${externalReferenceCode},${categoryId},${parentCategoryId},${titleMap},${descriptionMap},${vocabularyId}");

		var portalURL = JSONCompany.getPortalURL();
		var userLoginInfo = JSONUtil2.formatJSONUser();

		var curl = '''
			${portalURL}/api/jsonws/assetcategory/update-category \
				-u ${userLoginInfo} \
				-d externalReferenceCode=${externalReferenceCode} \
				-d categoryId=${categoryId} \
				-d parentCategoryId=${parentCategoryId} \
				-d titleMap=${titleMap} \
				-d descriptionMap=${descriptionMap} \
				-d vocabularyId=${vocabularyId} \
				-d categoryProperties=
		''';

		com.liferay.poshi.runner.util.JSONCurlUtil.post(${curl});
	}

}