definition {

	@summary = "Default summary"
	macro _deleteLayoutUtilityPage(layoutUtilityPageEntryId = null) {
		Variables.assertDefined(parameterList = ${layoutUtilityPageEntryId});

		if (!(isSet(portalURL))) {
			var portalURL = JSONCompany.getPortalURL();
		}

		var userLoginInfo = JSONUtil2.formatJSONUser();

		var curl = '''
			${portalURL}/api/jsonws/layoututilitypage.layoututilitypageentry/delete-layout-utility-page-entry \
				-u ${userLoginInfo} \
				-d layoutUtilityPageEntryId=${layoutUtilityPageEntryId}
		''';

		JSONCurlUtil.post(${curl});
	}

	@summary = "Default summary"
	macro _getLayoutUtilityPageEntryId(groupId = null, layoutUtilityPageName = null, specificURL = null) {
		Variables.assertDefined(parameterList = "${groupId},${layoutUtilityPageName}");

		if (!(isSet(portalURL))) {
			var portalURL = JSONCompany.getPortalURL();
		}

		var userLoginInfo = JSONUtil2.formatJSONUser(domain = ${specificURL});

		var curl = '''
			${portalURL}/api/jsonws/layoututilitypage.layoututilitypageentry/get-layout-utility-page-entries \
				-u ${userLoginInfo} \
				-d groupId=${groupId}
		''';

		var layoutUtilityPageEntryId = JSONCurlUtil.post(${curl}, "$.[?(@['name'] == '${layoutUtilityPageName}')]['layoutUtilityPageEntryId']");

		if (${layoutUtilityPageEntryId} == "") {
			fail("FAIL. Cannot find utility page.");
		}

		return ${layoutUtilityPageEntryId};
	}

	@summary = "Default summary"
	macro _setDefaultLayoutUtilityPage(layoutUtilityPageEntryId = null, specificURL = null) {
		Variables.assertDefined(parameterList = ${layoutUtilityPageEntryId});

		if (!(isSet(portalURL))) {
			var portalURL = JSONCompany.getPortalURL();
		}

		var userLoginInfo = JSONUtil2.formatJSONUser(domain = ${specificURL});

		var curl = '''
			${portalURL}/api/jsonws/layoututilitypage.layoututilitypageentry/set-default-layout-utility-page-entry \
				-u ${userLoginInfo} \
				-d layoutUtilityPageEntryId=${layoutUtilityPageEntryId}
		''';

		JSONCurlUtil.post(${curl});
	}

	@summary = "Default summary"
	macro _unsetDefaultLayoutUtilityPage(layoutUtilityPageEntryId = null, specificURL = null) {
		Variables.assertDefined(parameterList = ${layoutUtilityPageEntryId});

		if (!(isSet(portalURL))) {
			var portalURL = JSONCompany.getPortalURL();
		}

		var userLoginInfo = JSONUtil2.formatJSONUser(domain = ${specificURL});

		var curl = '''
			${portalURL}/api/jsonws/layoututilitypage.layoututilitypageentry/unset-default-layout-utility-page-entry \
				-u ${userLoginInfo} \
				-d layoutUtilityPageEntryId=${layoutUtilityPageEntryId}
		''';

		JSONCurlUtil.post(${curl});
	}

}